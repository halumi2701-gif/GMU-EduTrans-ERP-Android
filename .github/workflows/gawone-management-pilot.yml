name: GAWONE Management Pilot M1.1

on:
  push:
    branches: [ gawone-management-pilot-build ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - name: Reconstruct proven M1 and apply M1.1 reliability hardening
        shell: bash
        run: |
          cat gawone-management-m0.small00 gawone-management-m0.small01 gawone-management-m0.small02 gawone-management-m0.small03 gawone-management-m0.small04 gawone-management-m0.small05 gawone-management-m0.small06 gawone-management-m0.small07 > gawone-management-m0-source.b64
          test "$(wc -c < gawone-management-m0-source.b64)" -eq 25152
          base64 -d gawone-management-m0-source.b64 > gawone-management-m0-source.zip
          unzip -t gawone-management-m0-source.zip >/dev/null
          unzip -q gawone-management-m0-source.zip
          python gawone-management-m0-compile-fix.py
          patch -p4 < gawone-management-m0-to-m1.patch
          python gawone-management-m1-1-hardening.py gawone-management-pilot
          test -f gawone-management-pilot/app/build.gradle.kts
          grep -q 'applicationId = "site.garsyanimultiusaha.gawone.management"' gawone-management-pilot/app/build.gradle.kts
          grep -q 'versionCode = 3' gawone-management-pilot/app/build.gradle.kts
          grep -q 'versionName = "0.2.1-pilot-m1.1"' gawone-management-pilot/app/build.gradle.kts
          grep -q 'AndroidKeyStore' gawone-management-pilot/app/src/main/java/site/garsyanimultiusaha/gawone/management/SessionStore.kt
          grep -q 'api.rpc("review_partner_document"' gawone-management-pilot/app/src/main/java/site/garsyanimultiusaha/gawone/management/MainActivity.kt
          grep -q 'api.rpc("review_partner_service"' gawone-management-pilot/app/src/main/java/site/garsyanimultiusaha/gawone/management/MainActivity.kt
          grep -q 'ReadErrorCard' gawone-management-pilot/app/src/main/java/site/garsyanimultiusaha/gawone/management/MainActivity.kt
          grep -q 'get_customer_runtime_config' gawone-management-pilot/app/src/main/java/site/garsyanimultiusaha/gawone/management/MainActivity.kt
          grep -q 'get_mitra_runtime_config' gawone-management-pilot/app/src/main/java/site/garsyanimultiusaha/gawone/management/MainActivity.kt
          grep -q 'setOf("DOCUMENT_PENDING","KYC_REVIEW","SKILL_REVIEW")' gawone-management-pilot/app/src/main/java/site/garsyanimultiusaha/gawone/management/MainActivity.kt
          if grep -q 'api.rpc("review_partner_application"' gawone-management-pilot/app/src/main/java/site/garsyanimultiusaha/gawone/management/MainActivity.kt; then echo 'Unverified partner application mutation must not ship in M1.1' >&2; exit 1; fi
          if grep -R -n "service_role" gawone-management-pilot/app/src/main/java; then echo 'Privileged secret marker found in Android source' >&2; exit 1; fi
          zip -qr GAWONE_Management_v0.2.1_PILOT_M1_1_SOURCE.zip gawone-management-pilot

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - name: Install Android SDK
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Static QA and compile
        working-directory: gawone-management-pilot
        run: gradle :app:lintRelease :app:assembleRelease --stacktrace

      - name: Verify Management M1.1 APK
        shell: bash
        run: |
          APK=gawone-management-pilot/app/build/outputs/apk/release/app-release.apk
          test -f "$APK"
          cp "$APK" GAWONE_Management_v0.2.1_PILOT_M1_1.apk
          unzip -t GAWONE_Management_v0.2.1_PILOT_M1_1.apk >/dev/null
          sha256sum GAWONE_Management_v0.2.1_PILOT_M1_1.apk | tee GAWONE_Management_v0.2.1_PILOT_M1_1_SHA256.txt
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump badging GAWONE_Management_v0.2.1_PILOT_M1_1.apk > GAWONE_Management_v0.2.1_PILOT_M1_1_BADGING.txt
          grep -q "package: name='site.garsyanimultiusaha.gawone.management' versionCode='3' versionName='0.2.1-pilot-m1.1'" GAWONE_Management_v0.2.1_PILOT_M1_1_BADGING.txt
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs GAWONE_Management_v0.2.1_PILOT_M1_1.apk > GAWONE_Management_v0.2.1_PILOT_M1_1_SIGNATURE.txt
          test -f gawone-management-pilot/app/build/reports/lint-results-release.html

      - uses: actions/upload-artifact@v4
        with:
          name: GAWONE_Management_v0.2.1_PILOT_M1_1
          if-no-files-found: error
          path: |
            GAWONE_Management_v0.2.1_PILOT_M1_1.apk
            GAWONE_Management_v0.2.1_PILOT_M1_1_SHA256.txt
            GAWONE_Management_v0.2.1_PILOT_M1_1_SOURCE.zip
            GAWONE_Management_v0.2.1_PILOT_M1_1_BADGING.txt
            GAWONE_Management_v0.2.1_PILOT_M1_1_SIGNATURE.txt
            gawone-management-pilot/app/build/reports/lint-results-release.html

  emulator_smoke:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: GAWONE_Management_v0.2.1_PILOT_M1_1

      - name: Prepare emulator reports
        shell: bash
        run: mkdir -p emulator-reports

      - name: Enable KVM permissions
        shell: bash
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Management M1.1 Android emulator gate
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          target: google_apis
          profile: pixel_4
          disable-animations: true
          emulator-options: -no-snapshot -noaudio -no-boot-anim -gpu swiftshader_indirect -no-window -dns-server 8.8.8.8
          script: bash gawone-management-emulator-smoke.sh

      - name: Upload emulator evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: GAWONE_Management_v0.2.1_PILOT_M1_1_EMULATOR_GATE
          if-no-files-found: warn
          path: emulator-reports
